# OS / editor
.DS_Store

# Local Claude Code settings (machine-specific)
.claude/

# Unrelated local data that happens to live in this folder
lancedb/

# Node (tests are zero-dependency, but just in case)
node_modules/
